---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: friendviz
  annotations:
    flux.weave.works/automated: 'true'
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: friendviz
    spec:
      containers:
      - name: friendviz
        image: 'cjblink1/friendviz:e0b369f44fcb13c952416a8ffa10b83eb7362dab'
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: friendviz-service
  labels:
    name: friendviz-service
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: friendviz
  type: ClusterIP

